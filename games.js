(async()=>{try{const games=collectPublishedGames(await getPublicCoursesOnline());$("#games").innerHTML=games.length?games.map(g=>`<article class="card">${g.thumbnail?`<img class="library-thumbnail" src="${escapeHtml(g.thumbnail)}">`:`<span class="card-icon">🎮</span>`}<span class="tag">${escapeHtml(g.difficulty||"Intermediate")} · ${Number(g.minutes||10)} min</span><h3>${escapeHtml(g.title||"Simulation")}</h3><p>${escapeHtml(g.description||g.course_title)}</p><a class="button" href="course.html?id=${encodeURIComponent(g.course_id)}">Open simulation</a></article>`).join(""):`<div class="empty-panel">Add a Unity Game block to a published course and it appears here.</div>`}catch(e){$("#games").textContent=e.message}})()